# Render 배포 설정 (무료 플랜 — 디스크 없음)
services:
  - type: web
    name: nyangsalpim
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /tmp/nyangsalpim
